requires 1.3.5
setcpu 140
setmem 0.7

convert pp_light -out=.process
cd .process

seqsubsky pp_light 2

register bkg_pp_light -2pass
seqapplyreg bkg_pp_light -drizzle -scale=1.0 -pixfrac=0.9 -framing=cog

stack r_bkg_pp_light rej 3 3  -norm=addscale -output_norm -weight=wfwhm -out=stack
